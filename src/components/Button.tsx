
import { Button as ShadcnButton } from "@/components/ui/button";

// Import the Button component to prevent a TypeScript error with the file name
// This component is just a re-export of the shadcn Button
export default ShadcnButton;
